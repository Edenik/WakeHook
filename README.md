# WakeHook

**The open alarm clock that any AI agent can set.**

An Android alarm clock built for people *and* AI agents. An authorized agent sets
an alarm remotely by writing to a file in storage you own; the app schedules a
real local alarm and confirms it back. No WakeHook server, no extra account.

Status: early development. Apache-2.0.
